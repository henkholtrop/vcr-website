<%= form_for @game do |f| %>
  <% if @game.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@game.errors.count, "error") %> prohibited
      this game from being saved:</h2>
    <ul>
    <% @team.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
    <p>
      <%= f.label :home_team %><br>
      <%= f.collection_select(:home_team, Team.all, :id, :title) %>
    </p>

    <p>
      <%= f.label :home_score %><br>
      <%= f.number_field :home_score %>
    </p>

    <p>
      <%= f.label :away_team %><br>
      <%= f.collection_select(:away_team, Team.all, :id, :title) %>
    </p>

    <p>
      <%= f.label :away_score %><br>
      <%= f.number_field :away_score %>
    </p>

    <p>
      <%= f.label :date %><br>
      <%= f.datetime_select :date %>
    </p>

    <p>
      <%= f.submit %>
    </p>
  </p>
<% end %>
<script>
  $(function() {
    $('div#edit').editable({
      inlineMode: false
    })
  });
</script>